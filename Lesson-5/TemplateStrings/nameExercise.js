// Write a function getName that takes a name argument,
// and then console.logs "Hello, <name>" with a template string

const getName = name => {
  console.log(`Hello, ${name}`);
}

getName('Nick');
